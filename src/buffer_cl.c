
#include "buffer_cl.h"

void buffer_cl_dty(struct buffer* self)
{
	struct buffer_cl_properties* props = self->properties;
	if(props != NULL)
	{
		//props->host_buffer->fn->dty(props->host_buffer);
	}
		
}
